export const one = style({
  color: vars.color.red,
  padding: vars.space['2x'],
  fontSize: "12px",
});

export const withTemplateString = style({
  margin: `${vars.space['2x']} ${vars.space['4x']}`,
});
